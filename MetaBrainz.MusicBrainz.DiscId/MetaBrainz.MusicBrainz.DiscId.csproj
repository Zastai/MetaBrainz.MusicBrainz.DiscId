<?xml version="1.0" encoding="utf-8"?>
<Project Sdk="Microsoft.NET.Sdk" ToolsVersion="15.0">
  <PropertyGroup>
    <TargetFrameworks>net35;net20</TargetFrameworks>
  </PropertyGroup>
  <Import Project="..\build\Common.props" />
  <Import Project="..\build\NuGetPackage.props" />
  <PropertyGroup>
    <Version>0.2.0-pre</Version>
    <AssemblyVersion>0.2</AssemblyVersion>
    <FileVersion>0.2.0</FileVersion>
    <Description>This package provides classes for accessing information about a CD (its MusicBrainz Disc ID, EAN/ISRC values, CD-TEXT info, ...).</Description>
    <PackageTags>MusicBrainz discid audio cd cd-text isrc upc ean libdiscid</PackageTags>
    <!-- Owners and Summary are currently arbitrarily left out of MSBuild -->
    <PackageSummary>.NET Implementation of libdiscid</PackageSummary>
  </PropertyGroup>
  <Import Project="..\build\NuGetPackage.targets" />
</Project>
